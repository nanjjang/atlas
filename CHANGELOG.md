# 변경 내역

## 0.1.4 - 2026-09-03

### 큰 워크스페이스에서 분석이 멈춘 것처럼 보이던 문제

- 업데이트 직후 확장이 느려졌다는 보고가 있었지만, 원인은 버전을 올린 커밋이 아니라 그 전부터 있던
  스캔 방식이었습니다. 워크스페이스 전체를 `**/*`로 나열해 이미지·아카이브·캐시까지 전부 찾아낸
  뒤에야 분석을 시작했고, 중간 진행 상황도 보여 주지 않아 큰 워크스페이스에서는 화면이 멈춘 것처럼
  보였습니다.
- `repogram.maxFiles` 기본값을 2,500 → 10,000으로 올리고, 파일 읽기 병렬 처리를 16 → 32로
  늘렸습니다.
- 기본 제외 목록에 `dist-test`, `.vscode-test`, `.vscode-test-web`을 추가했습니다. 테스트
  런타임과 테스트 빌드 산출물이 소스 파일 수보다 많아 분석을 붙잡고 있었습니다.

### node_modules는 개발자가 작성한 코드가 아닙니다

- 설치된 패키지까지 분석 대상에 들어가 있어, `repogram.exclude` 설정과 무관하게
  `node_modules` · `.git` · `.venv` 같은 의존성 · VCS 디렉터리를 탐색과 변경 감시 양쪽에서
  강제로 제외하도록 고쳤습니다. 워크스페이스가 좁은 사용자 정의 제외 패턴을 쓰면 그 패턴이 기본값을
  완전히 대체해 의존성 폴더가 다시 검색 대상에 들어올 수 있었기 때문에, 사용자 설정과 별개로
  항상 지켜지는 최소 제외 목록을 따로 두었습니다.
- 검색 자체도 워크스페이스 전체를 나열한 뒤 걸러내는 대신, Repogram이 실제로 읽는 소스 · 설정
  확장자만 대상으로 좁혔습니다.

### 웹뷰가 아무것도 그리지 않던 문제

- 분석은 끝났는데 패널에 결과가 표시되지 않는 문제가 있었습니다. 진짜 원인은 패널 HTML에
  `id="workspace"`가 빠져 있어 웹뷰 스크립트가 그 엘리먼트를 찾지 못하고 즉시 종료되던
  버그였습니다.
- id를 되돌려 놓고, 웹뷰가 초기 `ready` 메시지를 보내기 전에 호스트가 먼저 메시지 리스너를
  등록하도록 순서를 바꿔 두 이벤트가 어긋날 여지를 없앴습니다.
- 같은 문제가 조용히 재발하지 않도록, 웹뷰 스크립트가 찾는 모든 id가 패널 HTML에 실제로 있는지
  확인하는 계약 테스트를 추가했습니다.

### 좁은 화면에서 접힌 선택 상세

- 좁은 편집기에서 선택 상세를 접으면 큰 빈 영역이 남던 문제를 수정했습니다. 이제 좁은 화면에서는
  아래쪽 34px 막대로, 넓은 화면에서는 오른쪽 34px 막대로 접힙니다.
- 접기 방향에 맞게 좁은 화면의 화살표를 위·아래 방향으로 표시합니다.
- 반응형 접힘 크기를 검증하는 CSS 계약 테스트를 추가했습니다.

## 0.1.0 - 2026-09-01

### Interfaces 탭 — 프로토콜, 포트, 그리고 프로토콜별 엔드포인트

- 낯선 서비스에 대해 처음 묻는 것은 "이걸 어떻게 부르나"인데, 지금까지의 네 탭은 어느 것도 답하지
  않았습니다. Architecture는 모듈이 서로를 어떻게 부르는지를, Data model은 데이터가 어떤 모양인지를
  말할 뿐 **바깥에서 이 프로젝트로 들어오는 문**은 어디에도 없었습니다.
- 세 조각을 한 탭에 함께 둡니다. 셋은 서로를 설명합니다 — 프로토콜 없는 포트는 숫자고, 포트 없는
  엔드포인트는 아무도 부를 수 없는 경로입니다.
  - **Outside edges** — 이 프로젝트가 쓰는 프로토콜과 개수, 엔드포인트 총계, 포트 선언 수.
  - **Ports** — 포트 선언 전체를, **어떻게 선언되었는지**와 함께. `binds`(코드가 직접 여는 포트) ·
    `exposes`(이미지) · `publishes`(컴포즈 매핑) · `setting`(설정 기본값)은 믿을 만한 정도가 서로
    다르고, 그 차이가 이 목록에서 가장 중요한 정보입니다.
  - **프로토콜마다 한 장** — HTTP는 메서드와 경로, gRPC는 `패키지.서비스/메서드`, GraphQL은 루트 필드,
    Kafka·AMQP·MQTT·Redis는 토픽과 큐, WebSocket은 이벤트 이름. 각 행에 그 선언에 붙은 핸들러 이름이
    함께 붙고, 두 번 누르면 그 줄로 갑니다.
- **그림이 아니라 카드 대시보드입니다.** 엔드포인트에는 배치할 이웃이 없습니다. 여기서 의미가 있는 관계는
  "같은 프로토콜"과 "같은 포트" 둘뿐이고, 둘 다 상자 사이에 선을 긋는 것보다 행을 나란히 놓는 편이 잘
  말합니다.
- **읽는 곳**: 라우터 등록 · NestJS · Spring MVC · JAX-RS · ASP.NET · Flask · FastAPI · Django ·
  Rails · Symfony · gin · echo · chi · `net/http` · axum · actix · rocket · Next.js의 `route.ts`와
  `pages/api` · OpenAPI 문서, `.proto`, GraphQL 스키마, socket.io · `ws` · STOMP, Dockerfile
  `EXPOSE`, 컴포즈 `ports:`, 쿠버네티스 `containerPort`, `server.port` 같은 설정.
- **브로커 토픽은 그 파일이 무엇을 import했는지로 판단합니다.** `subscribe('orders')`는 Kafka에서도
  MQTT에서도 Redis에서도 똑같이 생겼으므로, 호출만 보고 프로토콜을 적는 것은 지어내는 것입니다.
  브로커를 알 수 없으면 그 줄은 아무 프로토콜에도 넣지 않습니다.
- **포트에 프로토콜을 붙일 때는 근거를 남깁니다.** 같은 파일에서 한 종류의 엔드포인트만 나오면 그
  프로토콜로 보고, 아니면 잘 알려진 번호로 짐작하되(5432 → PostgreSQL) 짐작한 이름을 행에 함께 적습니다.
  둘 다 아니면 `TCP / other`에 그대로 둡니다 — 대부분의 포트가 HTTP라는 이유로 HTTP라고 적는 것은
  아무 말도 안 하는 것보다 나쁩니다.
- **핸들러 이름은 그 라우트 바로 아래 선언일 때만 붙입니다.** 사이에 가드·상태 코드·문서화 애너테이션이
  몇 줄 끼는 것까지는 따라가고, 그보다 멀면 이름을 비웁니다. 엉뚱한 함수를 가리키는 것은 아무것도 적지
  않는 것보다 나쁩니다. 데코레이터가 붙은 핸들러는 시그니처가 여러 줄에 걸치거나 파라미터 안에 세미콜론이
  들어가는 일이 흔해서, 그 두 모양을 읽는 규칙을 따로 두었습니다.
- 스냅숏에 `interfaces`가 생기고 통계에 `protocols` · `endpoints` · `ports`가 늘었습니다. `.proto` ·
  `.graphql` · `.properties` · `Dockerfile.*` 같은 파일이 이제 분석 대상에 들어옵니다.

### Files 탭이 "어디부터 읽어야 하나"에 답합니다

- 전에는 모듈 그룹과 그 파일 수, 외부 의존성을 세는 **재고 목록**이었습니다. 저장소가 얼마나 큰지는
  말해 주지만 어디를 열어야 하는지는 말해 주지 않았고, 전체 파일 목록은 이미 Explorer에 있습니다.
- 낯선 저장소를 열었을 때 실제로 묻게 되는 다섯 가지를 **모듈이 아니라 파일 단위로** 짚는 카드가
  들어갔습니다.
  - **Start here** — 다른 파일에서 불려 가는 대신 프로그램을 시작하는 파일. 파일 트리에서
    `src/extension.ts`와 `src/icons.ts`는 똑같이 생긴 한 줄이라, 트리는 이 질문에 답할 수 없습니다.
  - **Most depended on** — 그 파일을 import하는 프로젝트 파일 수. 고치면 파장이 가장 멀리 가는 곳입니다.
  - **Largest source files** — 줄 수 기준. 400줄을 넘으면 숫자를 강조합니다. 길이는 결함이 아니라 읽는
    시간이 쌓이는 자리입니다.
  - **Nothing imports these** — 아무 파일도 import하지 않는 소스. **죽은 코드 판정이 아닙니다.** 빌드
    진입점 · 규약 로딩 · 동적 import가 정적 분석에서는 똑같아 보이므로 카드가 그 사실을 함께 적고,
    해석하지 못한 import가 있으면 그것도 알립니다.
  - **No test named after them** — 이름이 대응하는 테스트가 없는 소스. 폴더가 아니라 파일 이름으로
    맞추므로 테스트를 옆에 두는 관례와 따로 두는 관례에서 모두 동작합니다.
- 요약 카드에 **언어 구성 비율 막대**가 붙습니다. 비율은 상태 표시줄의 개수 범례가 보여 줄 수 없는
  것입니다. `other`는 아무리 커도 항상 마지막에 둡니다 — "Other 31%"로 시작하는 막대는 이 프로젝트가
  무엇으로 쓰였는지에 대해 아무 말도 하지 않습니다.
- **경로가 길면 파일 이름을 남기고 앞쪽 폴더를 줄입니다.** 벤더 트리에서는 모든 경로가 앞 마흔 자를
  공유해서, 뒤를 자르면 여덟 행이 전부 `webVersion/venvs/lib/pyth…`로 똑같이 보였습니다.
- 같은 파일이 여러 카드에 있으면 함께 선택되므로, 한 파일을 여러 각도에서 동시에 볼 수 있습니다.
- 파일마다 **줄 수 · 바이트 · 들어오는 import 수 · 나가는 import 수 · 역할**을 재서 기록합니다. import
  수는 아키텍처 분석이 이미 하고 나서 모듈로 뭉갤 때 버리던 해석 결과를 그대로 쓴 것입니다.

### 문서 데이터베이스(MongoDB 계열)를 읽습니다

- 지금까지의 스키마 리더는 모두 **읽어 올 카탈로그가 있다**고 전제했습니다. 문서 데이터베이스에는 그런
  것이 없습니다 — 쓰기를 검증하지 않으므로, 가진 스키마는 애플리케이션 코드가 지키기로 한 모양뿐입니다.
  그것도 실재하는 선언이므로 여섯 개의 리더가 그것을 읽습니다.
  - **Mongoose** · **Dynamoose** — `new Schema({…})`와 `model('X', schema)`. 선언 형태가 같아 파서를
    공유합니다. `ref:` 참조, 중첩 객체, 서브도큐먼트, `collection` 옵션을 읽습니다.
  - **Typegoose** — `@prop` 클래스, `@modelOptions`의 컬렉션 이름, `ref: () => X`.
  - **MongoEngine** — `Document` · `EmbeddedDocument` 상속, `ReferenceField` · `ListField`,
    `meta = {'collection': …}`.
  - **Beanie** — 타입 어노테이션, `Link[X]` · `BackLink[X]`, `Optional`, 중첩 `class Settings`.
  - **Spring Data MongoDB** — `@Document` 클래스, `@Id` · `@Field` · `@Indexed` · `@DBRef`.
- 관계형 리더와 **번갈아가 아니라 나란히** 돌아갑니다. 한 워크스페이스가, 가끔은 한 파일이 둘 다 담기
  때문입니다.
- **데이터베이스가 아무것도 강제하지 않는다는 사실을 그림이 함께 말합니다.** 분석당 한 번
  `DB_SCHEMALESS_SOURCE` 진단이 붙고, 카드마다 `Schema enforcement` 항목이 붙어, 컬렉션이 이 그림과
  다른 문서를 담을 수 있고 참조가 이미 사라진 문서를 가리킬 수 있다고 알립니다.
- 세는 단위가 **`collection`**으로 바뀝니다. 없는 테이블을 찾게 만들지 않기 위해서입니다. 관계형과
  문서가 섞인 워크스페이스에서는 많은 쪽을 따라 한 단어로 정합니다.
- **임베디드 문서를 컬렉션과 다르게 그립니다.** 모델이 등록되지 않은 스키마는 다른 문서 안에만 존재하는
  모양이므로 **점선 테두리와 흐린 머리글**로 그립니다 — 테두리는 읽을 수 있는 배율에서 정확하고,
  머리글은 스키마 전체를 한 화면에 맞춘 배율에서도 살아남습니다. 그것을 담는 선도 점선입니다. 참조는 한
  번 더 조회해야 하는 것이고 임베딩은 같은 문서라, 둘이 같아 보이면 안 됩니다.
- Spring Data는 전용 스캐너를 붙였습니다. **규약으로 매핑하므로 `@Document` 클래스의 필드 대부분에
  애너테이션이 없고**, 관계형 리더가 쓰는 "`@`에서 시작하는" 방식으로는 필드 12개짜리 문서가 2개로
  보고되었습니다. 메서드 본문은 필드를 읽기 전에 지웁니다 — `return name;`도 타입과 이름과 세미콜론이라
  필드처럼 보이기 때문입니다.

### 여러 줄로 쓴 import를 해석합니다

- JavaScript/TypeScript import를 읽는 패턴이 절을 `[^'"\n]`으로 묶어 두어, **한 줄로 쓴 문장만**
  잡았습니다. 이름 두 개를 넘어가면 포매터가 만들어 내는 여러 줄짜리 목록은 이 분석기에게 import가
  아니었습니다.
- 이 저장소에서는 그것이 절반을 날렸습니다 — `webview-src/main.ts`가 자기 import 8개 중 4개만
  해석되었습니다. 모듈 간 선이 적게 세어졌고, 여러 줄로 import하는 파일만이 자기를 부르던 파일은
  "아무도 import하지 않는 파일"처럼 보였습니다.
- 이제 절을 "줄바꿈이 아닌 아무것"이 아니라 **import 절을 이루는 문자**(이름 · 중괄호 · 쉼표 · `*` ·
  `as`)로 묶습니다. 줄바꿈을 허용하면서도, 게으른 매칭이 문장을 넘어가 다음 문장의 대상을 훔쳐 오지
  않습니다.

### Flow 탭을 순서도로 그립니다

- **다이아몬드는 흐름이 실제로 갈라지는 자리에만 그립니다.** 전에는 소스에 보이는 `if`를 전부 마름모로
  세웠고, 그중 어느 쪽으로도 선이 나가지 않는 것이 대부분이었습니다 — 44개 노드짜리 파일 범위에서
  다이아몬드 20개 중 19개가 그랬습니다. 답이 없는 질문의 벽이었고, 진짜 분기 몇 개가 그 안에
  묻혔습니다. 조건은 한쪽 가지에서 무언가에 도달하는 것이 확인된 순간에 다이아몬드가 됩니다. 같은 파일
  범위가 노드 44개·다이아몬드 20개에서 **노드 25개·다이아몬드 1개**가 되었습니다.
- 갈라지지 않은 조건과 상자를 줄 수 없었던 호출은 **그 단계의 카드 안에 줄로 적힙니다.** 엔터티 카드가
  컬럼을 싣는 것과 같은 방식입니다. 이름만 적힌 상자는 결국 파일을 열어야 알 수 있었습니다.
- 분기 라벨이 `Yes / branch`에서 **`Yes` / `No`**로 짧아지고, 선 위에 배경을 깔고 굵게 놓입니다.
- **아무것도 부르지 않고 아무도 부르지 않는 함수는 `Start here`와 `End`에 잇지 않습니다.** 경로 위에 없는
  것을 양쪽에 이으면 소스에 없는 흐름을 그리게 됩니다. 반대로 매달릴 갈래가 여덟 개를 넘으면 경계 노드
  자체를 그리지 않습니다. 라우트 36개짜리 서비스에서 그것은 화면에서 가장 바쁜 물건이면서 라우트 표시가
  이미 하는 말만 했습니다. 같은 서비스 범위가 관계 125개에서 **68개**가 되었습니다.
- **배치는 탭마다 고정이고, 고르는 버튼을 없앴습니다.** Flow는 언제나 **Columns**(순서도는 방향이
  전부입니다), Architecture와 Database는 언제나 **Spread**(읽는 순서가 없고, 열로 세우면 선 수백 개가
  한 점으로 모입니다). 배치는 취향이 아니라 그 다이어그램의 성질이라, `Auto` / `Columns` / `Spread`
  선택지는 그 다이어그램을 더 읽기 어렵게 만드는 방법만 제공하고 있었습니다. 툴바의 그 버튼은
  구조 탭에서만 쓰이던 **폴더 접기/펼치기**로 남고, 다른 탭에서는 보이지 않습니다.
- **선 위의 글자는 분기에만 남습니다.** `starts` · `dispatches` · `calls` · `finishes`가 모든 선에 붙어
  캔버스 글자 대부분을 차지했는데, 화살표가 이미 하는 말이었습니다. 이제 `Yes` / `No`만 그립니다.
  라벨 자체는 사라지지 않고 세부 정보 패널에 근거로 남습니다.
- **선언된 라우트와 다이어그램이 붙인 `Start here`를 구분합니다.** 둘 다 같은 종류로 기록돼 라우트가
  시작 표시와 똑같은 알약으로 그려지고 있었습니다. 라우트는 이제 **카드**로, `Start here`와 `End`는
  다이어그램 자신의 표시로 그립니다.
- **순서도의 모든 단계가 "제목 바 + 내용" 한 형태입니다.** 전에는 안에 적을 것이 있는 카드만 제목 바를
  받아, 같은 단계인데 다른 종류처럼 보였습니다. 적을 것이 없는 카드는 제목 바 높이로 끝납니다.
- **채워진 제목 바 위의 글자 색을 고쳤습니다.** 캔버스용 글자색이 그대로 쓰여 밝은 테마에서 묻혔습니다.
  데이터베이스·아키텍처 카드에도 같이 적용됩니다.
- **선과 라벨이 개수와 상관없이 그대로 남습니다.** 관계가 스물 몇 개를 넘으면 라벨을 감추고 선을 옅게
  까는 규칙이 있는데, 순서도에서 그것은 순서도를 이루는 두 가지를 다 지웁니다. 추론된 선은 여전히
  점선이지만 **눈으로 따라갈 수 있는 긴 점선**으로 그립니다.
- **단계 사이 간격을 순서도에 맞게 넓혔습니다.** 모듈 지도를 촘촘하게 유지하던 간격은 순서도에서는
  화살표와 그 라벨이 들어갈 자리를 남기지 않았습니다.
- 긴 흐름은 이제 전체 윤곽을 보여 주려고 축소해서 열지 않습니다. 순서도의 윤곽은 단계의 줄이므로,
  **글자가 읽히는 크기로 왼쪽 끝에서** 열고 따라 읽습니다.

### 다이어그램 배치가 구역의 가운데를 씁니다

- **구역은 그 구역의 이름이 된 카드를 가운데 두고 배치합니다.** 구역 대부분은 순서가 아니라 허브와
  위성입니다 — 테이블 하나와 거기에 키를 걸고 있는 테이블들, 모듈 하나와 그것을 import 하는 모듈들.
  열로 세우면 허브가 혼자 한 열에 서고 나머지가 옆 열에 쌓여서 구역 안의 모든 관계가 높이만 다른 같은
  가로선이 되었습니다. 이제 허브를 가운데 두고 이웃을 좌우로 나눠 세웁니다. 가운데라 할 만한 것이 없는
  구역은 그대로 열로 세웁니다.
- **묶어서 펼치는 배치의 문턱이 노드 24개에서 12개로 내려갔습니다.** 예전 문턱은 구역에 가운데가 없던
  시절의 것이고, 지금은 빽빽한 열두 개도 펼치는 편이 낫습니다. 테이블 17개짜리 주제 영역 — 실제 스키마가
  나뉘는 크기 대부분 — 이 문턱의 반대쪽에 걸려 있었습니다.
- **Modules와 Data 보기의 카드 간격을 넓혔습니다.** 격자 간격은 선을 지나보낼 일이 없는 카드를 기준으로
  잡혀 있었는데, 그것은 카드에 대해서는 맞고 그것을 구별해야 하는 사람에 대해서는 맞지 않았습니다. 한
  영역이 글자 덩어리 하나로 읽히고 있었습니다.
- 80개 테이블 기준으로 그림이 더 넓어졌지만 `fit`이 여는 배율은 **0.401에서 0.484로 21% 커졌습니다.**

### Data 보기는 주제 영역 단위로 읽습니다

- **스키마 전체를 한 장에 그리는 것을 기본에서 뺐습니다.** 테이블 백 몇십 개짜리 ERD는 "이 데이터베이스는
  크다"는 것 말고는 알려 주는 것이 없고, 정작 사람들이 스키마에 들고 오는 질문은 그리기 전과 똑같이
  어려운 채로 남습니다. 툴바에 **Area**가 생겼고, 30개가 넘는 스키마는 **영역 지도**로 먼저 열립니다.
  30개 이하면 예전처럼 통째로 열립니다.
- **영역 지도** — 영역 하나당 카드 하나, 카드에 테이블 수와 안에서 끝나는 관계 수, 밖으로 나가는 관계
  수를 적습니다. 두 영역 사이를 지나는 관계는 선 하나로 합치고 개수를 붙입니다. 카드를 두 번 누르면 그
  영역이 열립니다.
- **한 영역** — 그 영역의 테이블과 안에서 끝나는 관계는 그대로 그리고, 밖으로 나가는 관계는 **이웃 영역
  카드 하나로 접습니다.** 어느 테이블이 몇 개의 관계로 어디를 향하는지는 남고, 저쪽 영역의 테이블
  백 개는 그려지지 않습니다. 접힌 선도 자기가 선언된 컬럼은 그대로 들고 있어서, 카드의 외래 키 표시는
  상대가 화면 밖에 있어도 예전과 같습니다.
- **영역은 그래프에서 추측하지 않고 선언된 경계를 씁니다.** 스키마 파일, 이름에 붙은 스키마
  (`billing.invoices`), JPA 패키지, Django 앱 라벨. 혼자 두기에는 작은 영역은 상위 디렉터리로 접고,
  혼자서도 벽보만 한 영역은 **그 안의 테이블들이 전부 네임스페이스를 달고 있을 때만** 더 나눕니다.
  나눌 근거가 없으면 나누지 않고 "이 영역은 30개가 넘습니다"라고 적습니다.
- 이름이 겹치는 영역(`migration.sql`이 스무 개)은 자기 경로에서 겹치지 않는 가장 짧은 이름을 가져갑니다.
- 영역을 고르면 세부 정보에 **그 영역이 무엇인지가 글로도** 남습니다 — 읽어 온 파일, 테이블 수와 관계 수,
  맞닿은 영역, 그리고 그 영역의 **테이블 전체 목록**입니다. 이름을 누르면 그 테이블로 갑니다.
- 카드가 싣는 컬럼의 세 단계는 이제 **지금 그려진 그림**의 크기로 정해집니다. 스키마 전체로는 155개라도
  영역 하나가 마흔 개 이하면 그 영역에서는 컬럼이 전부 나옵니다.
- 관계 대상을 찾지 못한 자리 표시자는 **그것을 참조한 테이블이 있는 영역**에 놓습니다. 테이블이 아니므로
  영역 크기에는 세지 않습니다.
- 사이드바에서 테이블 행을 눌러 다이어그램을 열면, 그 테이블이 든 영역으로 범위를 옮긴 뒤 고릅니다.

### 스키마를 Markdown 문서로 내보냅니다

- Data 탭의 **Export** 버튼과 `Repogram: Export Schema Documentation` 명령이 생겼습니다. 그림
  파일이 아니라 **문서**를 씁니다 — 검색되고, diff로 볼 수 있고, 코드 옆에 열어 둘 수 있어야 실제로
  쓰이기 때문입니다.
- 문서에는 머리말(정적 분석이며 DB에 접속하지 않았다는 사실), 주제 영역 목차와 영역이 맞닿는 지점,
  영역마다 **Mermaid 다이어그램 하나**(30개 이하일 때만)와 **테이블·컬럼 표**, 각 테이블의 관계, 그리고
  선언을 찾지 못한 이름들과 분석 노트가 들어갑니다.
- 다이어그램은 `erDiagram`이 아니라 flowchart입니다. Mermaid의 ER 문법은 선마다 카디널리티를 적게
  하는데 정적 분석이 늘 알 수 있는 값이 아닙니다. 소스가 선언한 관계를 화살표에 적습니다.
- 선언을 찾지 못한 대상은 표에서 빠지고 다이어그램에서도 **다른 모양**으로 그려집니다. 있는 테이블처럼
  보이지 않게 하기 위해서입니다.
- 저장 위치는 **저장 대화상자로 직접 고릅니다.** 프로젝트가 문서를 어디에 두는지는 프로젝트의 사정이고,
  이 확장이 파일을 쓰는 곳은 여기 한 군데뿐입니다.

### 다이어그램 배치

- 관계가 촘촘하거나 대부분이 같은 몇 개 노드를 향하는 그래프는 열 대신 **묶음으로** 배치합니다.
  같은 곳에서 온 것끼리 테두리로 묶고, 묶음 안은 격자로, 묶음끼리는 줄로 늘어놓습니다.
  - **묶음은 추측하지 않습니다.** 테이블은 스키마 파일에, 모듈은 디렉터리에 이미 적혀 있습니다.
    테두리에 `billing.sql` 같은 이름이 붙는 이유이고, 외래 키는 대개 같은 파일 안을 가리키므로
    선의 대부분이 테두리 안에서 끝납니다 — 밖으로 나가는 몇 개가 실제로 볼 가치가 있는 선입니다.
    선언된 묶음이 없을 때만 관계에서 이웃을 찾고, 너무 작은 묶음은 가장 많이 얽힌 쪽에 접습니다.
  - 처음에는 힘 기반(force) 배치를 썼는데, 간선은 짧아져도 결과가 **쏟아 놓은 것처럼** 보였습니다.
    아무것도 줄이 맞지 않고 경계가 없어 눈으로 훑을 순서가 없었습니다. 지금은 시뮬레이션이 아니라
    구성으로 배치합니다 — 결정적이고, 엔터티 150개 기준 213ms에서 3ms로 줄었으며,
    `Fit` 배율이 0.25에서 0.54로 두 배 이상 커졌습니다.
- 카드가 싣는 컬럼이 세 단계가 됐습니다. 40개 이하면 전부, 220개 이하면 **기본 키와 외래 키만**,
  그 이상이면 이름만입니다. 관계를 따라갈 때 눈이 찾는 것이 키이고, 도착한 선이 가리키는 것도 그 컬럼입니다.
- 툴바의 **배치** 버튼으로 `Auto` / `Columns` / `Spread`를 직접 고를 수 있습니다. 기본값 `Auto`는 그래프 모양을 보고 정합니다.
- **간선을 직교(直交) 라우팅으로 그립니다.** 곡선이 아니라 직각으로 꺾이며, ERD·클래스 다이어그램 도구가 쓰는 방식과 같습니다.
  - 선이 노드의 **포트**에서 출발합니다. 한 테이블에서 나가는 스무 개의 선이 테두리의 스무 지점에서 각각 출발하므로, 시작점에서 이미 어느 선이 어느 것인지 구분됩니다.
  - 선이 노드 위를 지나지 않고 노드 **사이 통로**로 갑니다. A\* 탐색이 박스를 장애물로 두고 길을 찾으므로, 라벨 위에 선이 그어지는 일이 없습니다.
  - 앞선 선이 이미 쓴 통로는 비용이 붙어, 같은 방향으로 가는 두 번째 선은 옆 통로로 밀립니다. 나란한 선들이 겹치지 않고 레일처럼 읽힙니다.
  - 모서리는 둥글게 처리합니다. 직각으로 만나는 두 선분의 꼭짓점은 그 자체로 하나의 표식처럼 보이는데, 꼭짓점이 삼백 개면 화살표와 경쟁합니다.
  - 엔터티 123개·관계 278개 기준으로 라우팅에 6–17ms가 들고, 노드를 가로지르는 선분은 0개입니다.
- 그래프 대부분이 참조하는 노드의 간선은 옅게 깔고, 그 개수를 노드 위에 적습니다. 포인터를 올리거나 선택하면 원래 굵기로 돌아오고, 세부 정보 패널에는 언제나 전부 나열됩니다.
- 엔터티가 40개를 넘으면 카드는 이름만 싣고 컬럼 목록은 세부 정보 패널로 옮깁니다.

### Files 보기

- 폴더 구조를 `tree`가 그리는 것과 같은 **트리**로 그립니다. 한 줄에 한 항목, 폴더가 파일보다 먼저 오고, 각 폴더에서 그 안의 마지막 항목까지 선이 내려옵니다. 네 단계쯤 들어가면 들여쓰기만으로는 이 파일이 어느 폴더 소속인지 세어 봐야 알 수 있는데, 그 선이 답을 대신합니다.
- **행마다 아이콘이 섭니다.** 접힘 상태를 나타내는 꺾쇠, 열림/닫힘이 구분되는 폴더, 그리고 파일 종류를 나타내는 아이콘(`<>` 소스, `{}` 설정, 원기둥 스키마, 물방울 스타일시트, `>_` 스크립트 등)입니다. 언어별 색은 이름이 아니라 이 아이콘이 답니다 — 이름은 읽는 것이고 아이콘은 훑는 것이라, 훑을 때 필요한 정보가 아이콘 쪽에 있어야 합니다. 상태 표시줄에 색과 언어를 짝지은 범례가 나옵니다.
- 폴더 이름은 파랗고 굵게, 그 안의 파일 수가 옆에 섭니다.
- 캔버스가 아니라 **스크롤되는 목록**입니다. 에디터 글꼴을 그대로 쓰고, `+` / `−`가 글자 크기를 조절하며 `Fit`이 원래 크기로 되돌립니다.
- 폴더나 꺾쇠를 누르면 접히고 펴집니다. `Alt`를 누른 채 누르면 그 아래 전부가, 툴바의 **Collapse** / **Expand**로는 트리 전체가 한 번에 접히고 펴집니다.
- 편집 중인 파일을 따라 폴더를 여는 것은 **그 파일이 바뀔 때 한 번뿐**입니다. 다시 그릴 때마다 열었더니 지금 열려 있는 파일이 든 폴더는 접어도 곧바로 다시 펴져, 사실상 접을 수 없었습니다.
- 검색은 흐리게 만드는 대신 걸러 냅니다. 맞는 항목과 그 항목이 들어 있는 폴더만 남고, 실제로 맞은 항목에는 표시가 붙습니다.
- 방향키로 오르내리고 `←` `→`로 접고 펴며, `Enter`로 파일을 엽니다. 편집 중인 파일이 접힌 폴더 안에 있으면 그 폴더를 펴서 데려옵니다.

### 사이드바

- Files 탭의 폴더 구조도 다이어그램과 같은 트리로, 같은 아이콘으로 그립니다. 다만 선은 글자가 아니라 테두리로 긋습니다 — 글리프는 제 글꼴 크기만큼만 높아서, 22px 행에 13px 글자로 찍으면 세로선이 점선처럼 끊깁니다.
- 관계를 커밋 그래프처럼 레인에 그리던 방식을 걷어냈습니다. 대신 출처별로 묶고 연결이 많은 순으로 정렬하며, 관계 수를 행 뒤 막대로 깔았습니다. 레인이 쓰던 폭은 이름이 가져갑니다.
- 행 앞의 ▸로 그 항목의 관계를 펼치고, 관계를 눌러 상대 항목으로 이동합니다. 필터에 가려진 항목으로 이동하면 필터를 해제합니다.
- 관계 수는 필터와 무관하게 전체 그래프 기준으로 셉니다.

### 언어

- **Go를 제대로 읽습니다.** 이전에는 임포트가 하나도 연결되지 않았습니다. Go의 임포트 경로는 파일이 아니라
  **패키지, 즉 디렉터리**를 가리키는데 파일 하나로 찾고 있었기 때문입니다. 이제 그 디렉터리의 모든 `.go` 파일에
  연결합니다(`_test.go`는 임포트로 도달할 수 없으므로 제외).
  - 저장소에 `go.mod`가 여러 개 있으면 각각의 모듈 경로로 나눠 해석합니다.
  - `cmd` · `internal` · `pkg`를 모듈 경계로 인식합니다. `cmd/api`, `internal/store`가 모듈이 되고
    `cmd`, `internal`은 관례일 뿐입니다.
- **Dart와 Flutter를 인식합니다.** `import` · `export` · `part`를 읽고, `package:<이름>/…`은
  `pubspec.yaml`이 선언한 이름을 통해 그 패키지의 `lib/` 아래로 해석합니다. 점 없이 쓰는 Dart의 상대 임포트
  (`import 'foo.dart';`)도 형제 파일로 처리하고, `dart:` SDK는 의존성도 미해결도 아닌 것으로 둡니다.
- **외부 의존성을 모든 언어에서 수집합니다.** 이전에는 `isJavaScriptLike` 안에 갇혀 JS 계열에서만 모았고,
  Python · Java · C# · Rust · Ruby · PHP · Go · Dart 프로젝트는 전부 의존성이 하나도 없는 것처럼 보였습니다.
  이제 언어마다 그 생태계가 패키지를 부르는 단위로 답니다.
  - **Go** — 첫 구간에 점이 있는지로 표준 라이브러리와 모듈을 가릅니다(`net/http`는 의존성이 아니고
    `github.com/…`은 의존성입니다). Go 툴체인 자신의 규칙입니다. 이름은 `github.com`이 아니라
    `github.com/gin-gonic/gin`처럼 모듈 경로 전체로 답니다 — 이전 규칙이었다면 모든 Go 의존성이
    호스팅 사이트 이름 하나로 뭉쳤을 것입니다.
  - **Dart** — `package:flutter/material.dart`를 `flutter`로. `dart:` SDK는 제외합니다.
  - **Python** — 첫 점 구간(`django.db.models` → `django`), 표준 라이브러리 제외.
  - **JVM** — 임포트 끝의 타입 이름을 떼고 라이브러리 패키지를 남깁니다
    (`org.springframework.boot.SpringApplication` → `org.springframework.boot`).
    `java` · `javax` · `jakarta` · `kotlin` · `scala` · `android`는 플랫폼입니다.
  - **C#** — 앞 두 구간(`Microsoft.Extensions`, `Newtonsoft.Json`), `System.*` 제외.
  - **Rust** — `crate` · `self` · `super`가 아닌 `use`와 `extern crate`를 새로 읽습니다.
    이전에는 이 구문 자체를 추출하지 않아 Rust 프로젝트의 크레이트가 한 개도 잡히지 않았습니다.
  - **Ruby** — `require 'active_support/core_ext'` → `active_support`, 표준 라이브러리 제외.
  - **PHP** — 벤더 네임스페이스(`Symfony\Component`, `Doctrine\ORM`).
  - **Swift** — 플랫폼 프레임워크를 뺀 나머지(`Alamofire`).
  - **C / C++** — 워크스페이스가 제공하지 못한 꺾쇠 include. 디렉터리로 배포되는 라이브러리는
    그 이름으로(`boost`, `fmt`, `nlohmann`), 낱개 헤더는 자기 이름으로 답니다.
  - 목록에 없는 표준 라이브러리 모듈은 의존성처럼 보일 수 있습니다. 규칙을 넓게 잡아 실제 의존성이 조용히
    걸러지는 쪽보다, 읽는 사람이 알아보고 넘길 수 있는 항목이 하나 더 보이는 쪽이 낫다고 봤습니다.
- **Swift를 인식합니다.** `import`와 `@testable import`를 읽고, SwiftPM의 `Sources/<타깃>/` 배치에서
  타깃을 모듈로 잡습니다. Foundation · SwiftUI · UIKit · XCTest · PackageDescription 같은 플랫폼
  프레임워크는 의존성이 아닙니다.
- **C와 C++을 인식합니다.** `#include`를 읽되 따옴표와 꺾쇠를 구분합니다 — 따옴표는 그 파일이 있는
  디렉터리부터, 꺾쇠는 워크스페이스의 `include` 트리부터 찾습니다. 주석 처리된 `#include`는 세지 않습니다.
  `<vector>` · `<stdio.h>` · `<sys/stat.h>`는 플랫폼이고, `<boost/asio.hpp>`는 `boost` 의존성입니다.
- **모듈 경계를 프로젝트가 시작되는 곳부터 셉니다.** 이전에는 최상위 한 단계만 봐서, Go 서버와 Flutter
  클라이언트가 함께 있는 저장소가 `backend`와 `client` 두 덩어리로 접히고 그 안의 구조 — 다이어그램의
  본체 — 가 통째로 사라졌습니다. 이제 매니페스트(`go.mod` · `pubspec.yaml` · `package.json` ·
  `Cargo.toml` · `pom.xml` · `Package.swift` 등)가 있는 디렉터리를 프로젝트의 시작으로 보고 그 안쪽에서
  경계를 읽습니다.
  - `backend/cmd/api`, `backend/internal/model`, `client/lib/data`가 각각 모듈이 됩니다.
  - 매니페스트가 저장소 루트에만 있으면 결과는 예전과 같습니다.
  - `Sources` · `Tests` · `include`도 컨테이너 이름으로 인식합니다(`Sources/App`, `include/engine`).
- SwiftPM · CMake · Meson을 빌드 시스템으로 표시합니다.
- Files 보기에서 Dart가 `C-family`가 아니라 자기 색과 이름을 갖습니다.
- Flutter · Riverpod · Bloc · Drift · Floor · Isar를 `pubspec.yaml`에서, Gin · Echo · Fiber · chi ·
  GORM · Ent · Bun을 `go.mod`에서 읽어 기술 목록에 올립니다. Go 모듈 경로에는 메이저 버전이 붙으므로
  (`.../echo/v4`) 접두사로 대조합니다.

### 데이터

- **GORM(Go) 엔터티를 읽습니다.** `gorm`을 임포트한 파일의 구조체만 대상으로 삼습니다 — 그렇지 않으면
  워크스페이스의 모든 요청 페이로드와 설정 구조체가 스키마에 올라옵니다.
  - `gorm:"…"` 태그(컬럼명 · 기본키 · 유니크 · NOT NULL · 기본값 · `foreignKey` · `many2many`),
    `gorm.Model` 임베딩이 더하는 네 컬럼, `TableName()` 재정의를 반영합니다.
  - 관계의 외래 키는 **그것을 실제로 가진 쪽**에 표기합니다. belongs-to는 이쪽, has-many는 상대쪽입니다.
  - 태그가 없으면 GORM의 `<필드>ID` 관례로 추측하되, 그 컬럼이 구조체에 실제로 없으면 표기하지 않습니다.
    없는 컬럼 이름을 지어내지 않기 위해서입니다.
- **Drift(Flutter) 테이블을 읽습니다.** `Table`을 상속한 클래스의 컬럼 빌더 체인을 그대로 읽습니다 —
  `autoIncrement()` · `nullable()` · `unique()` · `named()` · `references()`가 각각 ERD에 필요한 것을
  말하므로, 게터의 반환 타입이 아니라 체인이 선언입니다. `tableName`과 `primaryKey` 재정의도 반영합니다.
- 엔터티 이름과 테이블 이름이 대소문자만 다를 때(`Categories`가 `categories`로 저장되는, 대부분의 ORM
  기본값) 그 엔터티가 **자기 자신과 이름이 겹친다고 보고되던 버그**를 고쳤습니다. 별칭을 소문자로 접기
  **전에** 중복을 제거하고 있었습니다.

### 분석

- 기본 제외 패턴에 `.turbo`, `.nx`, `.cache`, `.parcel-cache`, `.gradle`, `.pnpm-store`, `.yarn`, `.vercel`, `.output`, `.angular`, `.dart_tool`, `bin`, `obj`를 추가했습니다. 프로젝트 자기 파일보다 많아 Files 보기를 통째로 덮던 캐시 디렉터리들입니다.

## 0.1.0 — 베타

첫 공개 버전입니다.

### 보기

- 활동 표시줄에 **Repogram** 사이드바가 생깁니다. 아이콘을 누르면 명령 없이 바로 분석이 시작되고 개요가 표시됩니다.
- 개요는 한 항목이 한 줄인 컴팩트 목록이고, 관계는 왼쪽 좁은 레인에 선으로 그립니다. Modules · Files · Data 세 탭을 오갑니다.
- 외부 패키지는 모듈 그래프와 섞이지 않고 접히는 목록으로 따로 모입니다.
- 에디터 영역의 다이어그램 패널은 그대로 있습니다. 사이드바 행의 ⤢ 버튼으로 해당 노드를 선택한 채 열 수 있습니다.

### 편집 중인 파일 따라가기

- 파일을 열면 개요가 그 파일의 모듈 행을 표시하고, Files 탭에서는 부모 폴더를 펼쳐 화면 안으로 가져옵니다.
- 다이어그램의 **Focus**는 편집 중인 모듈과 직접 연결된 이웃만 남기고 나머지를 뒤로 물린 뒤, 그 범위에 화면을 맞춥니다.
- 다이어그램 패널이 열려도 커서는 에디터에 남습니다.

### 분석

- TypeScript, JavaScript, Python, Java, Kotlin, C#, Rust, PHP, Ruby, Go의 import 관계.
- Prisma, SQL DDL, TypeORM, JPA, Django ORM 엔터티와 관계.
- Java 패키지 와일드카드(`import a.b.*`)는 해당 패키지 전체에 연결되며, 도달하는 모듈마다 한 번씩만 셉니다.
- 외부 패키지로 향하는 간선은 그 패키지를 실제로 쓰는 모듈 안의 파일을 가리킵니다.

### 갱신

- 자동 갱신이 `repogram.exclude`를 따르므로, 빌드 산출물에 쓰기가 발생해도 다시 분석하지 않습니다.
- 창을 다시 열면 다이어그램 패널이 보기·선택·확대 상태까지 복원됩니다.
- 사이드바와 다이어그램이 함께 열려 있어도 워크스페이스 스캔은 한 번만 수행합니다.

### 알려진 한계

정적 분석이라 런타임에 만들어지는 관계는 놓칩니다. 자세한 내용은 README의 "이 확장이 하지 않는 일"을 보십시오.
